{
  "name": "ticketing-backend",
  "version": "1.0.0",
  "description": "Backend for ticketing system with WhatsApp OTP forgot password",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --watch server.js --watch config --watch controllers --watch routes --watch services --ignore data --ignore .wwebjs_auth --ignore .wwebjs_cache --ext js,json"
  },
  "keywords": [
    "ticketing",
    "whatsapp",
    "otp",
    "authentication"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "whatsapp-web.js": "^1.23.0",
    "qrcode-terminal": "^0.12.0",
    "body-parser": "^1.20.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
